// Template for ai_config.dart — copy to `ai_config.dart` and paste your key.
//
// Get a free key at https://aistudio.google.com -> API keys.
//
// NOTE: this key is compiled into the app and CAN be extracted from the APK.
// Treat it as public: restrict it in Google Cloud Console (API restriction:
// "Generative Language API") and rotate it if it leaks. Leave empty to disable
// AI logging. `ai_config.dart` is gitignored so the real key isn't committed.

const String kGeminiApiKey = '';
