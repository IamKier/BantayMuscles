// Template for remote_config.dart — copy this file to `remote_config.dart`
// and paste in your Supabase project's values.
//
//   Dashboard -> Project Settings -> Data API   (URL)
//   Dashboard -> Project Settings -> API Keys   (anon / publishable key)
//
// The anon/publishable key is safe to ship in a client — row-level security is
// what protects the data. Never put the service_role key here.
//
// Leave both empty to run fully offline against the bundled food list.
// `remote_config.dart` is gitignored so real values never get committed.

const String kSupabaseUrl = '';
const String kSupabaseAnonKey = '';
